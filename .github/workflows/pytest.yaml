# This workflow will install Python dependencies, run tests and lint with a variety of Python versions
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: tests

on:
    push:
        branches: ["main"]
    pull_request:
        branches: ["main", "mdlm"]

jobs:
    build:
        runs-on: ${{ matrix.os }}
        strategy:
            fail-fast: false
            matrix:
                os: [ubuntu-latest, ubuntu-22.04]
                python-version: ["3.10", "3.11", "3.12", "3.13"]

        steps:
            - uses: actions/checkout@v4
            - name: Set up Python ${{ matrix.python-version }}
              uses: actions/setup-python@v5
              with:
                  python-version: ${{ matrix.python-version }}
                  cache: pip
                  cache-dependency-path: pyproject.toml
            - name: Install project and test deps
              run: |
                  python -m pip install --upgrade pip setuptools wheel
                  python -m pip install "torch==2.7.1+cpu" --extra-index-url https://download.pytorch.org/whl/cpu
                  python -m pip install "torch-cluster==1.6.3+pt27cpu" --only-binary=:all: --find-links https://data.pyg.org/whl/torch-2.7.1+cpu.html
                  python -m pip install -e .
                  python -m pip install pytest
            - name: Test with pytest
              run: |
                  pytest
